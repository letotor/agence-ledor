{
  "name": "chapter7-agence-ledor",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:letotor/agence-ledor",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.html --open",
    "clean" : "rm rf dist/*",
    "deploy" : "netlify build && netlify deploy --prod && netlify watch"
    
  },
  "devDependencies": {
    "netlify-cli": "^10.0.0",
    "parcel-bundler": "^1.12.5"
  },
  "dependencies": {
    "global": "^4.4.0",
    "parcel": "^2.5.0"
  }
}
